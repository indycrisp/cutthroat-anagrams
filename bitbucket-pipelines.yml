image: node:8.1.2

pipelines:
  default:
    - step:
        name: "Deploy to production"
        deployment: production
        script:
          - ssh root@104.131.28.152
          - cat deploy.sh
          - echo "Deploy finished"
